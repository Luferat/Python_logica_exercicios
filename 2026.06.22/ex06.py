'''
6. Encapsulamento Simples: crie uma classe chamada Employee com os requisitos:

Atributo privado: __salary
Métodos:

get_salary()
set_salary(value)

Regras:

O salário não pode ser negativo.

Exemplo:

employee.set_salary(5000)
print(employee.get_salary())

Dica
Use o prefixo __ para tornar o atributo privado.
'''